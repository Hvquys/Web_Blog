@model List<BE_Blog.Comment>
@if (Model.Count > 0)
{
    for (int i = 0; i < Model.Count; i++)
    {
        var item = Model[i];
        <div class="card-body">
            <h5 class="card-title">@item.AuthorName</h5>
            <p class="card-text">@item.Content</p>
        </div>
    }
}

